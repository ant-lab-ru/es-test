name: 1.1.1 Установка инструментов

on:
  push:
    paths:
      - "tools.log"
      - ".github/workflows/check-1-1-1.yml"
  workflow_dispatch:

jobs:
  check:
    uses: ant-lab-ru/es-test/.github/workflows/check-1-1-1.yml@master
